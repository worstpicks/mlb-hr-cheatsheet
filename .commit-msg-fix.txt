Fix batter name matching and rebuild June 2 sheet with full prop coverage.

Normalize PropFinder lineup-slot prefixes and initial periods so all 92 HR props match CSV data, and refresh summary sections with updated counts.
